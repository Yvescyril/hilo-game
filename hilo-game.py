
from random import random



class hilo:
    # each card stands for a digit from 1 to 13
    # the role of hilo is to determine whether the value of card hiher or
    # lower but still from the same range

    def __init__(Self):
        # builds the instance for the hilo
        Self.cardvalue = 0
    def cardPoint(self):
        #generating value and then compute the points
        self.cardPoint= random.randint(1,13)
class refree:
    # that's just the one who is going to lead the game
    def __init_(self):
        pass
        self.card1= 0